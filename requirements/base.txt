# Graph store
neo4j==5.28.1

# Vector store (entry-point search only, not primary context source)
chromadb==1.5.9

# Embeddings (BGE-m3, tái dùng pattern từ rag-chatbot-document-QA)
sentence-transformers==5.6.1

# Serving
fastapi==0.141.1
uvicorn==0.52.1
httpx==0.28.1

# Config / env loading
python-dotenv==1.2.2

# Testing
pytest==9.1.1
