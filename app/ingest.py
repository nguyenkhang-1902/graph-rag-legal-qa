"""Entry point CLI cho ingest pipeline (batch + savepoint, 67k văn bản).

Stub — logic thật được implement ở T009d (Phase 2: Foundational), sau khi
`graph_store/upsert.py` (T009) và `ingest_checkpoint/state_store.py` (T009b)
đã có. Xem `specs/001-graph-rag-core/plan.md` và `research.md` ADR-002.
"""


def main() -> None:
    raise NotImplementedError("app/ingest.py sẽ được implement ở T009d")


if __name__ == "__main__":
    main()
